# MongoDB Connection
DB_URL=mongodb+srv://username:password@cluster.mongodb.net/database_name?appName=Cluster0

# Client URL
CLIENT_URL=http://localhost:5173

# JWT Secrets (generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_ACCESS_SECRET=your_access_secret_here
JWT_REFRESH_SECRET=your_refresh_secret_here

# SMTP Settings (optional, для email функционала)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# API_URL=http://localhost:3001
