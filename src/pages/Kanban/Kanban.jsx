import { Board } from "../../components/Kanban/Board";

export const Kanban = () => {
    return (
        <section className="h-full w-full bg-neutral-900 text-neutral-50 rounded-4xl">
            <Board />
        </section>
    );
};
